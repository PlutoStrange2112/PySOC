environment         = "dev"
location            = "eastus"
retention_days      = 90
defender_tier       = "Free"
soc_email_addresses = ["soc-dev@example.com"]

# Set via environment variables or CI/CD secrets — do NOT commit real values
# ci_principal_id   = "..."
# virustotal_api_key = "..."
